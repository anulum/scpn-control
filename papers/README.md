# Papers

This directory is the authoritative SCPN-CONTROL manuscript collection.
Numbered packages under [`submissions/`](submissions/) contain the source,
bibliography, build instructions, citation metadata, and inspected review PDF
for each distinct paper.

The repository does not infer external submission or publication from the
presence of a review package. Current status is recorded in each package's
metadata.
